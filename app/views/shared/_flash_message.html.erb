<% flash.each do |message_type, message| %>
  <div class="alert alert-dismissible fade show 
    <%= message_type.to_s == 'notice' ? 'alert-success' : 'alert-danger' %>" role="alert">
    <%= message %>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close">
    </button>
  </div>
<% end %>